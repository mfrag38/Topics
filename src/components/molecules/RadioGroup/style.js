import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	groupTitleContainer: { marginLeft: 10 },
	groupTitleText: { color: '#000', fontSize: 18, fontWeight: 'bold' },
});
